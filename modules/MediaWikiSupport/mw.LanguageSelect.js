/**
 * Simple language selection uses. 
 */

mw.LanguageSelect = {
	/**
	 * Get the language box ( note this c
	 */
	get: function( options ){
		// Build a select object 
		// TODO test string construction instead of jQuery build out for performance  
		$j('<select id="combobox">

		return $langSelect;
	}	
};