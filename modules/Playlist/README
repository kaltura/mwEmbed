The Media RSS module provides basic mediaRss parsing and playlist display. 

It in includes a basic jquery-ui themeable gui. 

The main options that can be set after the Kaltura JavaScript include:

// Here are the defaults ( only layout right now ) 

// MediaRssLayout can be "horizontal" or "vertical"
mw.setConfig( 'MediaRss.Layout', 'horizontal' ); 
