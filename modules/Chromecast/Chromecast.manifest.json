{
    "chromecast": {
        "description": "Enable casting the video stream to Google Chromecast.",
        "attributes": {
            "parent": {
                "doc": "Parent container for component. Components include default placement, leave as null if unsure.",
                "type": "enum",
                "enum": [
                    "topBarContainer",
                    "controlsContainer"
                ],
                "options": [
                    {
                        "label": "Top bar container",
                        "value": "topBarContainer"
                    },
                    {
                        "label": "Controls container",
                        "value": "controlsContainer"
                    }
                ],
                "initvalue": "controlsContainer"
            },
            "applicationID": {
                "label": "Application ID",
                "doc": "The Chromecast Application ID. Leave empty if default Kaltura receiver is used",
                "type": "string"
            }
        }
    }
}